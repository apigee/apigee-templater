name: auth-openai-key
displayName: OpenAI Key Auth
type: feature
description: Validates OpenAI API keys (sk-*), resolves groups and credentials from AI-Users and AI-Config KVMs, caches user/key details, and sets resolved key in x-ai-key and group in x-ai-group.
documentation: ""
priority: 5
categories:
  - llm
  - ai
parameters: []
defaultEndpoint:
  name: default
  basePath: /openai-key
  routes:
    - name: default
  flows:
    - name: PreFlow
      mode: Request
      steps:
        - name: KVM-LoadConfig
        - name: AM-RemoveGroupHeader
          condition: request.header.x-ai-group != null
        - name: JS-ExtractOpenAIKey
        - name: LC-LookupTokenCache
          condition: openai.raw_api_key != null
        - name: JS-ParseCachedUser
          condition: openai.key.cached_user != null
        - name: KVM-GetGroupsByKey
          condition: openai.raw_api_key != null AND openai.key_cached != "true"
        - name: JS-ProcessUser
          condition: openai.raw_api_key != null AND openai.key_cached != "true"
        - name: PC-PopulateTokenCache
          condition: openai.raw_api_key != null AND openai.key.cached_user != null AND openai.key_cached != "true"
    - name: PostFlow
      mode: Response
      steps: []
  faultRules: []
endpoints: []
targets: []
policies:
  - name: AM-RemoveGroupHeader
    type: AssignMessage
    content:
      assignMessage:
        metadata:
          continueOnError: "false"
          enabled: "true"
          name: AM-RemoveGroupHeader
        displayName: AM-RemoveGroupHeader
        properties: {}
        remove:
          headers:
            header:
              metadata:
                name: x-ai-group
        ignoreUnresolvedVariables: "true"
        assignTo:
          metadata:
            createNew: "false"
            transport: http
            type: request
  - name: JS-ExtractOpenAIKey
    type: Javascript
    content:
      javascript:
        metadata:
          continueOnError: "true"
          enabled: "true"
          timeLimit: "200"
          name: JS-ExtractOpenAIKey
        displayName: JS-ExtractOpenAIKey
        properties: {}
        source: |
          var xApiKey = context.getVariable("request.header.x-api-key");
          var authHeader = context.getVariable("request.header.Authorization");
          var rawKey = null;

          function isOpenAiKey(key) {
            return key && key.trim().indexOf("sk-") === 0 && key.trim().indexOf("sk-ant") !== 0;
          }

          if (xApiKey && isOpenAiKey(xApiKey)) {
            rawKey = xApiKey.trim();
          } else if (authHeader) {
            authHeader = authHeader.trim();
            if (authHeader.indexOf("Bearer ") === 0) {
              var token = authHeader.substring(7).trim();
              if (isOpenAiKey(token)) {
                rawKey = token;
              }
            } else if (isOpenAiKey(authHeader)) {
              rawKey = authHeader;
            }
          }

          if (rawKey) {
            context.setVariable("openai.raw_api_key", rawKey);
            context.setVariable("openai.groups_kvm_key", "groups." + rawKey);
          }
  - name: LC-LookupTokenCache
    type: LookupCache
    content:
      lookupCache:
        metadata:
          continueOnError: "true"
          enabled: "true"
          name: LC-LookupTokenCache
        displayName: LC-LookupTokenCache
        properties: {}
        cacheKey:
          keyFragment:
            - value: openai_key
            - metadata:
                ref: openai.raw_api_key
        assignTo: openai.key.cached_user
  - name: JS-ParseCachedUser
    type: Javascript
    content:
      javascript:
        metadata:
          continueOnError: "true"
          enabled: "true"
          timeLimit: "200"
          name: JS-ParseCachedUser
        displayName: JS-ParseCachedUser
        properties: {}
        source: |
          var cachedUserStr = context.getVariable("openai.key.cached_user");
          if (cachedUserStr) {
            try {
              var cached = JSON.parse(cachedUserStr);
              if (cached.raw_api_key) context.setVariable("openai.raw_api_key", cached.raw_api_key);
              if (cached.groups) context.setVariable("openai.key.groups", cached.groups);
              if (cached.api_key) context.setVariable("request.header.x-ai-key", cached.api_key);
              if (cached.group) context.setVariable("request.header.x-ai-group", cached.group);
              context.setVariable("openai.key_cached", "true");
              print("OpenAI user info successfully restored from cache");
            } catch (e) {
              print("Failed to parse cached OpenAI data: " + e);
            }
          }
  - name: KVM-GetGroupsByKey
    type: KeyValueMapOperations
    content:
      keyValueMapOperations:
        metadata:
          continueOnError: "true"
          enabled: "true"
          name: KVM-GetGroupsByKey
          mapIdentifier: AI-Users
        displayName: KVM-GetGroupsByKey
        properties: {}
        get:
          metadata:
            assignTo: openai.key.groups
            index: "1"
          key:
            parameter:
              metadata:
                ref: openai.groups_kvm_key
        scope: environment
  - name: JS-ProcessUser
    type: Javascript
    content:
      javascript:
        metadata:
          continueOnError: "false"
          enabled: "true"
          timeLimit: "200"
          name: JS-ProcessUser
        displayName: JS-ProcessUser
        properties: {}
        source: |
          var groupData = context.getVariable("openai.key.groups");
          var resolvedKey = null;
          var matchedGroup = null;

          if (groupData) {
            var groupArray = groupData.split(",");
            var groupCredentials = context.getVariable("ai.groups");
            if (groupCredentials) {
              try {
                var groupCredentialsObject = JSON.parse(groupCredentials);
                for (var i = 0; i < groupArray.length; i++) {
                  var grp = groupArray[i].trim();
                  if (groupCredentialsObject[grp]) {
                    resolvedKey = groupCredentialsObject[grp];
                    matchedGroup = grp;
                    print("Found group credential for OpenAI key, setting as x-ai-key...");
                    break;
                  }
                }
              } catch(e) {
                print("Error parsing group credential data: " + e);
              }
            }
            if (!matchedGroup && groupArray.length > 0 && groupArray[0].trim() !== "") {
              matchedGroup = groupArray[0].trim();
            }
          }

          if (!resolvedKey) {
            var rawKey = context.getVariable("openai.raw_api_key");
            if (rawKey) resolvedKey = rawKey;
          }

          if (resolvedKey) {
            context.setVariable("request.header.x-ai-key", resolvedKey);
          }
          if (matchedGroup) {
            context.setVariable("request.header.x-ai-group", matchedGroup);
          }

          var rawApiKey = context.getVariable("openai.raw_api_key") || "";
          var groups = context.getVariable("openai.key.groups") || "";
          var apiKey = context.getVariable("request.header.x-ai-key") || "";
          var groupHeader = context.getVariable("request.header.x-ai-group") || "";

          var cacheObject = {
            raw_api_key: rawApiKey,
            groups: groups,
            api_key: apiKey,
            group: groupHeader
          };
          context.setVariable("openai.key.cached_user", JSON.stringify(cacheObject));
  - name: PC-PopulateTokenCache
    type: PopulateCache
    content:
      populateCache:
        metadata:
          continueOnError: "true"
          enabled: "true"
          name: PC-PopulateTokenCache
        displayName: PC-PopulateTokenCache
        properties: {}
        cacheKey:
          keyFragment:
            - value: openai_key
            - metadata:
                ref: openai.raw_api_key
        source: openai.key.cached_user
        expirySettings:
          timeoutInSec: "3600"
  - name: KVM-LoadConfig
    type: KeyValueMapOperations
    content:
      keyValueMapOperations:
        metadata:
          continueOnError: "false"
          enabled: "true"
          name: KVM-LoadConfig
          mapIdentifier: AI-Config
        displayName: KVM-LoadConfig
        properties: {}
        exclusiveCache: "false"
        expiryTimeInSecs: "10"
        get:
          - metadata:
              assignTo: ai.failoverModel
              index: "1"
            key:
              parameter: FailoverModel
          - metadata:
              assignTo: ai.prices
              index: "1"
            key:
              parameter: PriceList
          - metadata:
              assignTo: ai.groupsLookup
              index: "1"
            key:
              parameter: GroupsLookup
          - metadata:
              assignTo: ai.groups
              index: "1"
            key:
              parameter: Groups
        scope: environment
resources: []
tests: []
